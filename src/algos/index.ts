import { astr } from "./astr";
import { bfs } from "./bfs";
import { dfs } from "./dfs";

// Obiekt zawierający wszystkie dostępne algorytmy
const Algorithms = {
  astr,
  bfs,
  dfs,
};

// Eksport obiektu zawierającego algorytmy
export { Algorithms };
